# Versioning

## pipespec_version

PipeSpec documents MUST include:

- `pipespec_version: "1.0"`

## metadata.schema_version (deprecated)

Historic datasets may include:

- `metadata.schema_version`

This field is supported for backward compatibility but should not be used going forward.